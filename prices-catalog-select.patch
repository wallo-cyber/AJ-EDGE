diff --git a/app/app/prices/page.tsx b/app/app/prices/page.tsx
index 31f2061..cd50591 100644
--- a/app/app/prices/page.tsx
+++ b/app/app/prices/page.tsx
@@ -285,15 +285,19 @@ export default function PricesPage() {
     return [...set].sort((a, b) => a.localeCompare(b, 'ar'));
   }, [rows, vendorById]);
 
-  /** أسماء البنود المستخدمة سابقًا — لتوحيد الكتابة عند الإدخال */
+  /** أسماء البنود المستخدمة سابقًا + بنود دليل البنود — لتوحيد الكتابة عند الإدخال */
   const itemSuggestions = useMemo(() => {
     const set = new Set<string>();
+    catalogItems.forEach((c) => {
+      const i = safe(c.item);
+      if (i) set.add(i);
+    });
     rows.forEach((r) => {
       const i = safe(r.item);
       if (i) set.add(i);
     });
     return [...set].sort((a, b) => a.localeCompare(b, 'ar'));
-  }, [rows]);
+  }, [rows, catalogItems]);
 
   const filtered = useMemo(() => {
     const q = query.trim();
@@ -377,6 +381,7 @@ export default function PricesPage() {
     setSaving(true);
     setError('');
     try {
+      const matchedCatalog = matchCatalogItem(draft.item.trim());
       const values: Record<string, unknown> = {
         vendor_id: draft.vendor_id,
         item: draft.item.trim(),
@@ -385,6 +390,7 @@ export default function PricesPage() {
         quoted_at: draft.quoted_at || todayISO(),
         source: draft.source || null,
         notes: draft.notes.trim() || null,
+        catalog_item_id: matchedCatalog ? matchedCatalog.id : null,
       };
       if (editingId) {
         values.updated_at = new Date().toISOString();
@@ -1095,7 +1101,15 @@ export default function PricesPage() {
                 className={field}
                 list="price-items"
                 value={draft.item}
-                onChange={(e) => setDraft({ ...draft, item: e.target.value })}
+                onChange={(e) => {
+                  const value = e.target.value;
+                  const matched = matchCatalogItem(value);
+                  setDraft((d) => ({
+                    ...d,
+                    item: value,
+                    unit: !d.unit && matched && safe(matched.unit) ? safe(matched.unit) : d.unit,
+                  }));
+                }}
                 placeholder="مثال: خرسانة مسلحة للقواعد شاملة الحديد والصب"
               />
               <datalist id="price-items">
@@ -1104,8 +1118,8 @@ export default function PricesPage() {
                 ))}
               </datalist>
               <p className="mt-1 text-[11px] text-[var(--nav-secondary)]">
-                اكتب البند بالصيغة نفسها كل مرة — القائمة تقترح ما سجّلته سابقًا حتى تتجمّع العروض
-                تحت بند واحد.
+                اكتب البند بالصيغة نفسها كل مرة — القائمة تقترح ما سجّلته سابقًا وبنود دليل البنود
+                حتى تتجمّع العروض تحت بند واحد.
               </p>
             </div>
 
